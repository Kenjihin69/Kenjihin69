
spawn(function()
local light = Instance.new("Highlight", game.Players.LocalPlayer.Character)
local cc = Instance.new("ColorCorrectionEffect", game.Lighting)
light.OutlineTransparency = 1
light.FillTransparency = 0
light.FillColor = Color3.new(0, 0, 0)
cc.Brightness = 1
wait(0.03)
light.FillColor = Color3.new(255, 255, 255)
cc.Brightness = -1
wait(0.03)
cc:Destroy()
light:Destroy()
end)
task.wait(0)
loadstring(game:HttpGet('https://raw.githubusercontent.com/Kenjihin69/Kenjihin69/refs/heads/main/Yujihit'))()
task.wait(0)
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local head = character:WaitForChild("Head") -- Ensure the "Head" exists

-- Load the sword model
local swordModel = game:GetObjects("rbxassetid://72043190110868")[1]

-- Ensure the sword model has a handle
local handle = swordModel:FindFirstChild("Handle") or swordModel:FindFirstChildWhichIsA("BasePart")
if not handle then
    error("The sword model must have a 'Handle' or main part to attach.")
end

-- Set the sword's parent to the character
swordModel.Parent = character

-- Weld the sword to the head
local weld = Instance.new("Weld")
weld.Part0 = head
weld.Part1 = handle

-- Adjust the sword's position and orientation to align properly with the head
weld.C0 = CFrame.new(0, -0.4, 0) * CFrame.Angles(0, math.rad(180), math.rad(0))
weld.Parent = head

-- Ensure the sword's orientation matches the intended alignment
handle.CFrame = head.CFrame * weld.C0
task.wait(0)
local skillOne = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Cursed Strikes'].ItemName
local skillTwo = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Crushing Blow'].ItemName
local skillThree = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].ItemName
local skillThreeTip = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].Tip
local skillFour = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Manji Kick'].ItemName

local ultTitle = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Title
local UltText = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Ready.Ready
local UIGradientCustom = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.UIGradient
UltText.Text = "The True Cursed"

UIGradientCustom.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0), Color3.fromRGB(255, 0, 0))

game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local TweenService = game:GetService("TweenService")
local bar = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill

local darkGrey = Color3.fromRGB(139, 0, 0)
local white = Color3.fromRGB(139, 0, 0)

local tweenInfo = TweenInfo.new(
	3,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut, 
	-1, 
	true 
)

local tweenbar = TweenService:Create(bar, tweenInfo, {BackgroundColor3 = darkGrey})

tweenbar:Play()

skillOne.Text = "Barrage Cursed"
skillTwo.Text = "Slam Dunk"
skillThree.Text = "Divergent Flash"
skillThreeTip.Text = "Click Again"
skillFour.Text = "Manji Kicks"
ultTitle.Text = "The Cursed"


-- SKILL 1 --

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local cursedStrikesANIM = ReplicatedStorage.Animations.Itadori.CursedStrike
local NewAnim = game:GetService("ReplicatedStorage").Animations.Choso.WingKing

local SWAP_DELAY = 0.7
local PLAY_DURATION = 0.6

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
		sounds.S1:Play()
local Sound = 78658251839338
local Volume = 4
local Speed = 1

s= Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
s.SoundId = "rbxassetid://" .. Sound
s:Play()
s.Volume = Volume
s.PlaybackSpeed = Speed	
task.wait(0)
local Test = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.r_VFX.Middle

local test = Test:Clone()
test.Parent = game.Players.LocalPlayer.Character:WaitForChild("Right Arm")

for _, child in ipairs(test:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(0)
local Ken = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.r_VFX.Middle

local ken = Ken:Clone()
ken.Parent = game.Players.LocalPlayer.Character:WaitForChild("Left Arm")

for _, child in ipairs(ken:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(0)
local Sigma = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.l_VFX.Accent

local sigma = Sigma:Clone()
ken.Parent = game.Players.LocalPlayer.Character:WaitForChild("Right Arm")

for _, child in ipairs(sigma:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(0)
local Ligma = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.l_VFX.Accent

local ligma = Ligma:Clone()
ligma.Parent = game.Players.LocalPlayer.Character:WaitForChild("Left Arm")

for _, child in ipairs(ligma:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(2.5)
animTracks.anim1:Stop()
test:Destroy()
ken:Destroy()
sigma:Destroy()
ligma:Destroy()
task.wait(0)
local skillOne = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Cursed Strikes'].ItemName
local skillTwo = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Crushing Blow'].ItemName
local skillThree = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].ItemName
local skillThreeTip = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].Tip
local skillFour = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Manji Kick'].ItemName

local ultTitle = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Title
local UltText = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Ready.Ready
local UIGradientCustom = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.UIGradient
UltText.Text = "The True Cursed"

UIGradientCustom.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0), Color3.fromRGB(255, 0, 0))

game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local TweenService = game:GetService("TweenService")
local bar = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill

local darkGrey = Color3.fromRGB(139, 0, 0)
local white = Color3.fromRGB(139, 0, 0)

local tweenInfo = TweenInfo.new(
	3,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut, 
	-1, 
	true 
)

local tweenbar = TweenService:Create(bar, tweenInfo, {BackgroundColor3 = darkGrey})

tweenbar:Play()

skillOne.Text = "Barrage Cursed"
skillTwo.Text = "Slam Dunk"
skillThree.Text = "Divergent Flash"
skillThreeTip.Text = "Click Again"
skillFour.Text = "Manji Kicks"
ultTitle.Text = "The Cursed"

		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == cursedStrikesANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

---skill 2

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local crushingBlowANIM = ReplicatedStorage.Animations.Itadori.CrushingBlow
local NewAnim = game:GetService("ReplicatedStorage").Animations.Megumi.Mahoraga.Earthquake

local SWAP_DELAY = 0.5
local PLAY_DURATION = 0.7

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
		sounds.S1:Play()
		task.wait(1.2)
		animTracks.anim1:Stop()
		task.wait(0)
local skillOne = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Cursed Strikes'].ItemName
local skillTwo = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Crushing Blow'].ItemName
local skillThree = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].ItemName
local skillThreeTip = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].Tip
local skillFour = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Manji Kick'].ItemName

local ultTitle = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Title
local UltText = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Ready.Ready
local UIGradientCustom = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.UIGradient
UltText.Text = "The True Cursed"

UIGradientCustom.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0), Color3.fromRGB(255, 0, 0))

game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local TweenService = game:GetService("TweenService")
local bar = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill

local darkGrey = Color3.fromRGB(139, 0, 0)
local white = Color3.fromRGB(139, 0, 0)

local tweenInfo = TweenInfo.new(
	3,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut, 
	-1, 
	true 
)

local tweenbar = TweenService:Create(bar, tweenInfo, {BackgroundColor3 = darkGrey})

tweenbar:Play()

skillOne.Text = "Barrage Cursed"
skillTwo.Text = "Slam Dunk"
skillThree.Text = "Divergent Flash"
skillThreeTip.Text = "Click Again"
skillFour.Text = "Manji Kicks"
ultTitle.Text = "The Cursed"
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

---3 skill

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local divergentFistANIM = ReplicatedStorage.Animations.Itadori.Variants.DivergentFist1
local NewAnim = game:GetService("ReplicatedStorage").Animations.Nanami.RatioBreaker.RatioBreak1

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = "rbxassetid://12761286504"
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		animTracks.anim1.TimePosition = 0
		animTracks.anim1:Play()
sounds.S1:Play()
               animTracks.anim1.TimePosition = 1
		
	        animTracks.anim1:AdjustSpeed(1)

local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local camera = game.Workspace.CurrentCamera


local slowZoomSpeed = 0.2
local fastZoomSpeed = 0.6
local zoomInFOV = 30
local defaultFOV = 70


local function zoomIn()
    local zoomInTween = TweenService:Create(
        camera,
        TweenInfo.new(slowZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.In),
        {FieldOfView = zoomInFOV}
    )
    zoomInTween:Play()
end


local function zoomOut()
    local zoomOutTween = TweenService:Create(
        camera,
        TweenInfo.new(fastZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
        {FieldOfView = defaultFOV}
    )
    zoomOutTween:Play()
end

task.wait(0)
        zoomIn()
local skillOne = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Cursed Strikes'].ItemName
local skillTwo = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Crushing Blow'].ItemName
local skillThree = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].ItemName
local skillThreeTip = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Divergent Fist'].Tip
local skillFour = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Manji Kick'].ItemName

local ultTitle = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Title
local UltText = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Ready.Ready
local UIGradientCustom = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.UIGradient
UltText.Text = "The True Cursed"

UIGradientCustom.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0), Color3.fromRGB(255, 0, 0))

game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local TweenService = game:GetService("TweenService")
local bar = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill

local darkGrey = Color3.fromRGB(139, 0, 0)
local white = Color3.fromRGB(139, 0, 0)

local tweenInfo = TweenInfo.new(
	3,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut, 
	-1, 
	true 
)

local tweenbar = TweenService:Create(bar, tweenInfo, {BackgroundColor3 = darkGrey})

tweenbar:Play()

skillOne.Text = "Barrage Cursed"
skillTwo.Text = "Slam Dunk"
skillThree.Text = "Divergent Flash"
skillThreeTip.Text = "Click Again"
skillFour.Text = "Manji Kicks"
ultTitle.Text = "The Cursed"
task.wait(0.5)
zoomOut()
task.wait(1.2)
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

----Ult
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local divergentFistANIM = ReplicatedStorage.Animations.Itadori.Ultimate
local mahitoUltANIM = ReplicatedStorage.Animations.Megumi.Mahoraga.Takedown
local cleaveFrANIM = ReplicatedStorage.Animations.Hakari.RoughEnergy
local ClimaxANIM = ReplicatedStorage.Animations.Todo.ClimaxJump
local ChaseANIM = ReplicatedStorage.Animations.Mahito.Variants.ChaseArmor
local SoundForm1 = 17765816057
local SoundForm2 = 300208109

local SWAP_DELAY = 0.1 
local PLAY_DURATION = .2

local isAnimationSequenceActive = false

local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	local animations = {
		mahitoUlt = Instance.new("Animation"),
		cleaveFr = Instance.new("Animation"),
		Climax = Instance.new("Animation"),
		Chase = Instance.new("Animation")
	}

	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	sounds.S1.SoundId = "rbxassetid://6665054578"
	sounds.S2.SoundId = "rbxassetid://3374531743"
	animations.mahitoUlt.AnimationId = mahitoUltANIM.AnimationId
	animations.cleaveFr.AnimationId = cleaveFrANIM.AnimationId
	animations.Climax.AnimationId = ClimaxANIM.AnimationId
	animations.Chase.AnimationId = ChaseANIM.AnimationId




	local animTracks = {
		mahitoUlt = humanoid:LoadAnimation(animations.mahitoUlt),
		cleaveFr = humanoid:LoadAnimation(animations.cleaveFr),
		Climax = humanoid:LoadAnimation(animations.Climax),
		Chase = humanoid:LoadAnimation(animations.Chase)
	}

	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

		animTracks.mahitoUlt:Play()
		sounds.S1:Play()
		task.wait(0.12)
		animTracks.mahitoUlt:Stop()
		animTracks.cleaveFr:Play()
		animTracks.cleaveFr:AdjustSpeed(2.5)
		
		task.wait(.1)
		animTracks.Climax:Play()
		animTracks.Climax.TimePosition = 5.7
		task.wait(.1)

		animTracks.cleaveFr:Stop()
		task.wait(1.2)

		sounds.S2:Play()
		animTracks.Chase:Play()
		animTracks.Chase:AdjustSpeed(.9)
		animTracks.Climax:Stop()
		task.wait(.8)
		animTracks.Chase:Stop()
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

---Ult 1
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local crushingBlowANIM = game:GetService("ReplicatedStorage").Animations.Itadori.Dismantle
local wideStrikeANIM = game:GetService("ReplicatedStorage").Animations.Itadori.ManjiKick
local chosoMelee4ANIM = game:GetService("ReplicatedStorage").Animations.Itadori.Dismantle

local SWAP_DELAY = 0.01
local PLAY_DURATION = .5 

local isAnimationSequenceActive = false

local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	local animations = {
		chosoMelee4 = Instance.new("Animation"),
		wideStrike = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	animations.chosoMelee4.AnimationId = chosoMelee4ANIM.AnimationId
	animations.wideStrike.AnimationId = wideStrikeANIM.AnimationId

	local animTracks = {
		chosoMelee4 = humanoid:LoadAnimation(animations.chosoMelee4),
		wideStrike = humanoid:LoadAnimation(animations.wideStrike)
	}

	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end
	animTracks.wideStrike:Play()
	animTracks.wideStrike.TimePosition = 4.2
	animTracks.wideStrike:AdjustSpeed(1.1)
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local camera = game.Workspace.CurrentCamera


local slowZoomSpeed = 0.2
local fastZoomSpeed = 0.7
local zoomInFOV = 100
local defaultFOV = 70


local function zoomIn()
    local zoomInTween = TweenService:Create(
        camera,
        TweenInfo.new(slowZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.In),
        {FieldOfView = zoomInFOV}
    )
    zoomInTween:Play()
end


local function zoomOut()
    local zoomOutTween = TweenService:Create(
        camera,
        TweenInfo.new(fastZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
        {FieldOfView = defaultFOV}
    )
    zoomOutTween:Play()
end
task.wait(0)
        zoomIn()
task.wait(0)
local Sound = 109660701821480
local Volume = 5
local Speed = 1

s= Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
s.SoundId = "rbxassetid://" .. Sound
s:Play()
s.Volume = Volume
s.PlaybackSpeed = Speed
task.wait(0)
local startTime = tick()  -- Get the current time in seconds Uh yep
local duration = 0  -- Duration in sec Heh

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

local floatSpeed = 6.5 -- Adjust speed 
local duration = 0.1 -- Duration in sec :whatthehellbro: 0.1 cuz yes

local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
if humanoidRootPart then
    local startTime = tick()
    while tick() - startTime < duration do
        wait(0) -- Controls how the loop runs googoogaga
        humanoidRootPart.CFrame = humanoidRootPart.CFrame + humanoidRootPart.CFrame.LookVector * floatSpeed * -1

    end
end
task.wait(0.3)
	animTracks.wideStrike:Stop()
task.wait(0)
	animTracks.chosoMelee4:Play()
animTracks.chosoMelee4.TimePosition = 2
animTracks.chosoMelee4:AdjustSpeed(1.2)
task.wait(0)
local skillOne = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Dismantle'].ItemName
local skillTwo = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Open'].ItemName
local skillThree = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Rush'].ItemName
local skillThreeTip = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Rush'].Tip
local skillFour = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Malevolent Shrine'].ItemName

local ultTitle = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Title
local UltText = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Ready.Ready
local UIGradientCustom = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.UIGradient
UltText.Text = "The True Cursed"

UIGradientCustom.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0), Color3.fromRGB(255, 0, 0))

game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local TweenService = game:GetService("TweenService")
local bar = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill

local darkGrey = Color3.fromRGB(139, 0, 0)
local white = Color3.fromRGB(139, 0, 0)

local tweenInfo = TweenInfo.new(
	3,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut, 
	-1, 
	true 
)

local tweenbar = TweenService:Create(bar, tweenInfo, {BackgroundColor3 = darkGrey})

tweenbar:Play()

skillOne.Text = "Dismantle Net"
skillTwo.Text = "Furnace"
skillThree.Text = "Rush"
skillThreeTip.Text = ""
skillFour.Text = "Malevolent Shrine"
ultTitle.Text = "The Cursed"
	task.wait(0.7)
animTracks.chosoMelee4:Stop()
zoomOut()

	task.wait(PLAY_DURATION)
	isAnimationSequenceActive = false

	for _, track in pairs(animTracks) do
		track:Stop()
	end
end


local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

---fuga 
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local divergentFistANIM = game:GetService("ReplicatedStorage").Animations.Itadori.FlameArrow
local NewAnim = game:GetService("ReplicatedStorage").Animations.Yuta.Veilstep

local SWAP_DELAY = 0.3
local PLAY_DURATION = 1

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		animTracks.anim1.TimePosition = 0
		animTracks.anim1:Play()
               animTracks.anim1.TimePosition = 0
		sounds.S1:Play()
               animTracks.anim1:AdjustSpeed(1.3)
local Sound = 78637547122675
local Volume = 5
local Speed = 1

s= Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
s.SoundId = "rbxassetid://" .. Sound
s:Play()
s.Volume = Volume
s.PlaybackSpeed = Speed
task.wait(0)
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local camera = game.Workspace.CurrentCamera


local slowZoomSpeed = 0.2
local fastZoomSpeed = 0.2
local zoomInFOV = 100
local defaultFOV = 70


local function zoomIn()
    local zoomInTween = TweenService:Create(
        camera,
        TweenInfo.new(slowZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.In),
        {FieldOfView = zoomInFOV}
    )
    zoomInTween:Play()
end


local function zoomOut()
    local zoomOutTween = TweenService:Create(
        camera,
        TweenInfo.new(fastZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
        {FieldOfView = defaultFOV}
    )
    zoomOutTween:Play()
end
task.wait(0)
        zoomIn()
task.wait(0.2)
local startTime = tick()  -- Get the current time in seconds Uh yep
local duration = 0  -- Duration in sec Heh

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

local floatSpeed = 6.5 -- Adjust speed 
local duration = 0.1 -- Duration in sec :whatthehellbro: 0.1 cuz yes

local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
if humanoidRootPart then
    local startTime = tick()
    while tick() - startTime < duration do
        wait(0) -- Controls how the loop runs googoogaga
        humanoidRootPart.CFrame = humanoidRootPart.CFrame + humanoidRootPart.CFrame.LookVector * floatSpeed * -1

    end
end
task.wait(0.2)
local animationId = "70840150456007"

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

local animation = Instance.new("Animation")
animation.AnimationId = "rbxassetid://" .. animationId
local animationTrack = humanoid:LoadAnimation(animation)

animationTrack:Play()
animationTrack.TimePosition = 2
animationTrack:AdjustSpeed(1.4)

local skillOne = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Dismantle'].ItemName
local skillTwo = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Open'].ItemName
local skillThree = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Rush'].ItemName
local skillThreeTip = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Rush'].Tip
local skillFour = game.Players.LocalPlayer.PlayerGui.Main.Moveset['Malevolent Shrine'].ItemName

local ultTitle = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Title
local UltText = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Ready.Ready
local UIGradientCustom = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.UIGradient
UltText.Text = "The True Cursed"

UIGradientCustom.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0), Color3.fromRGB(255, 0, 0))

game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill.BackgroundColor3 = Color3.fromRGB(255, 0, 0)

local TweenService = game:GetService("TweenService")
local bar = game.Players.LocalPlayer.PlayerGui.Main.Ultimate.Bar.Fill

local darkGrey = Color3.fromRGB(139, 0, 0)
local white = Color3.fromRGB(139, 0, 0)

local tweenInfo = TweenInfo.new(
	3,
	Enum.EasingStyle.Sine,
	Enum.EasingDirection.InOut, 
	-1, 
	true 
)

local tweenbar = TweenService:Create(bar, tweenInfo, {BackgroundColor3 = darkGrey})

tweenbar:Play()

skillOne.Text = "Dismantle Net"
skillTwo.Text = "Furnace"
skillThree.Text = "Rush"
skillThreeTip.Text = ""
skillFour.Text = "Malevolent Shrine"
ultTitle.Text = "The Cursed"
task.wait(2)
animationTrack:Stop()
zoomOut()
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)
--m1
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local crushingBlowANIM = game:GetService("ReplicatedStorage").Animations.Itadori.Melee.Melee1
local NewAnim = game:GetService("ReplicatedStorage").Animations.Haruta.Ambush

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
		sounds.S1:Play()
animTracks.anim1:AdjustSpeed(1.1)
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rightArm = character:FindFirstChild("Right Arm") or character:WaitForChild("RightHand")

local sword = game:GetObjects("rbxassetid://118146955816239")[1]
local handle = sword:FindFirstChild("Handle") or sword:FindFirstChildWhichIsA("BasePart")
if not handle then return end

for _, part in ipairs(sword:GetDescendants()) do
    if part:IsA("BasePart") then
        part.CanCollide = false

        local highlight = Instance.new("Highlight")
        highlight.Adornee = part
        highlight.Parent = part
        highlight.FillTransparency = 1
        highlight.OutlineColor = Color3.new(0, 0, 0)
        highlight.OutlineTransparency = 0.4
    end
end

sword.Parent = character

local weld = Instance.new("Weld")
weld.Part0 = rightArm
weld.Part1 = handle
weld.C0 = CFrame.new(0, -1, 1) * CFrame.Angles(0, math.rad(0), math.rad(0))
weld.Parent = rightArm

handle.CFrame = rightArm.CFrame * weld.C0
task.wait(0)
local Sound = 96045121549069
local Volume = 1
local Speed = 1

s= Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
s.SoundId = "rbxassetid://" .. Sound
s:Play()
s.Volume = Volume
s.PlaybackSpeed = Speed
		task.wait(0.9)
		animTracks.anim1:Stop()
sword:Destroy()
		task.wait(0)

		--MOVESET END 
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

---m2 
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local divergentFistANIM = game:GetService("ReplicatedStorage").Animations.Itadori.Melee.Melee2
local NewAnim = game:GetService("ReplicatedStorage").Animations.Choso.Melee.Melee3

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		animTracks.anim1.TimePosition = 0
		animTracks.anim1:Play()
		sounds.S1:Play()
               animTracks.anim1:AdjustSpeed(1)
local Sigma = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.l_VFX.Accent

local sigma = Sigma:Clone()
sigma.Parent = game.Players.LocalPlayer.Character:WaitForChild("Right Leg")

for _, child in ipairs(sigma:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(15) 
    end
end
task.wait(0)
local Ken = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.r_VFX.Middle

local ken = Ken:Clone()
ken.Parent = game.Players.LocalPlayer.Character:WaitForChild("Right Leg")

for _, child in ipairs(ken:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(0.4)
sigma:Destroy()
ken:Destroy()
		
		
	      
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

---m4
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local divergentFistANIM = game:GetService("ReplicatedStorage").Animations.Itadori.Melee.Melee4
local NewAnim = game:GetService("ReplicatedStorage").Animations.Yuki.Melee.Melee4

local SWAP_DELAY = 0.1
local PLAY_DURATION = 0.8

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		animTracks.anim1.TimePosition = 0
		animTracks.anim1:Play()
		sounds.S1:Play()
               animTracks.anim1:AdjustSpeed(1)
		task.wait(2)
		
	      
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)
--domain
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local divergentFistANIM = game:GetService("ReplicatedStorage").Animations.Itadori.MalevolentShrine
local NewAnim = game:GetService("ReplicatedStorage").Animations.Heian.MalevolentShrine

local SWAP_DELAY = 0.1
local PLAY_DURATION = 0.7

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		animTracks.anim1.TimePosition = 0
		animTracks.anim1:Play()
                animTracks.anim1.TimePosition = 2.7
		sounds.S1:Play()
               animTracks.anim1:AdjustSpeed(0.2)
game.Players.LocalPlayer.Character.Humanoid.HipHeight = 10
task.wait(0)
local Duration = 1 -- (self explanetory)
local TitleText = "" -- the title in the middle 

-- dont touch this bullshit unless you know what r u doing
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local camera = workspace.CurrentCamera

local originalType = camera.CameraType
local originalCFrame = camera.CFrame

local gui = Instance.new("ScreenGui")
gui.Name = "CinematicBars"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.Parent = player:WaitForChild("PlayerGui")

local function createBar(yPos)
	local bar = Instance.new("Frame")
	bar.Size = UDim2.new(1, 0, 0.18, 0)
	bar.Position = UDim2.new(0, 0, yPos, yPos < 0.5 and -200 or 200)
	bar.BackgroundColor3 = Color3.new(0, 0, 0)
	bar.BorderSizePixel = 0
	bar.ZIndex = 10
	bar.Parent = gui
	return bar
end

local topBar = createBar(0)
local bottomBar = createBar(0.82)

local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(1, 0, 0.2, 0)
titleText.Position = UDim2.new(0, 0, 0.4, 0)
titleText.BackgroundTransparency = 1
titleText.Text = TitleText
titleText.TextColor3 = Color3.new(1, 1, 1)
titleText.TextStrokeColor3 = Color3.new(0, 0, 0)
titleText.TextStrokeTransparency = 0.5
titleText.TextScaled = true
titleText.Font = Enum.Font.GothamBlack
titleText.TextTransparency = 1
titleText.ZIndex = 11
titleText.Parent = gui

local tweenIn = TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.Out)
TweenService:Create(topBar, tweenIn, { Position = UDim2.new(0, 0, 0, 0) }):Play()
TweenService:Create(bottomBar, tweenIn, { Position = UDim2.new(0, 0, 0.82, 0) }):Play()
TweenService:Create(titleText, tweenIn, { TextTransparency = 0 }):Play()
task.wait(0.1)
local player = game.Players.LocalPlayer
local camera = game.Workspace.CurrentCamera
local character = player.Character or player.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local head = character:WaitForChild("Torso")

-- Save the current camera type and subject
local originalCameraType = camera.CameraType
local originalCameraSubject = camera.CameraSubject

-- Set the camera to Scriptable mode (cutscene mode)
camera.CameraType = Enum.CameraType.Scriptable

-- Get the character's CFrame and calculate the camera's offset
local radius = 6 -- Distance from the character to the camera
local angle = math.rad(45) -- 40 degrees forward

-- Calculate the position using trigonometry (left and forward)
local offset = humanoidRootPart.CFrame:VectorToWorldSpace(Vector3.new(-radius * math.cos(angle), 0.9, -radius * math.sin(angle)))

-- Set the camera's CFrame to the calculated position and make it look at the head
camera.CFrame = CFrame.new(head.Position + offset, head.Position)
task.wait(2)
local player = game.Players.LocalPlayer
local camera = game.Workspace.CurrentCamera
local character = player.Character or player.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local head = character:WaitForChild("Torso")

-- Save the current camera type and subject
local originalCameraType = camera.CameraType
local originalCameraSubject = camera.CameraSubject

-- Set the camera to Scriptable mode (cutscene mode)
camera.CameraType = Enum.CameraType.Scriptable

-- Get the character's CFrame and calculate the camera's offset
local radius = 3 -- Distance from the character to the camera
local angle = math.rad(45) -- 40 degrees forward

-- Calculate the position using trigonometry (left and forward)
local offset = humanoidRootPart.CFrame:VectorToWorldSpace(Vector3.new(-radius * math.cos(angle), 1, -radius * math.sin(angle)))

-- Set the camera's CFrame to the calculated position and make it look at the head
camera.CFrame = CFrame.new(head.Position + offset, head.Position)
task.wait(0.2)
local Sound = 124012721234128
local Volume = 4
local Speed = 1

s= Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
s.SoundId = "rbxassetid://" .. Sound
s:Play()
s.Volume = Volume
s.PlaybackSpeed = Speed
		task.wait(2)
		local workspace = game.Workspace
Players = game:GetService("Players")
local speaker = Players.LocalPlayer
workspace.CurrentCamera:remove()
wait(.0)
workspace.CurrentCamera.CameraSubject = speaker.Character:FindFirstChildWhichIsA('Humanoid')
workspace.CurrentCamera.CameraType = "Custom"
speaker.CameraMinZoomDistance = 0.5
speaker.CameraMaxZoomDistance = 400
speaker.CameraMode = "Classic"
speaker.Character.Head.Anchored = false
task.wait(0)	
	      game.Players.LocalPlayer.Character.Humanoid.HipHeight = 0
              animTracks.anim1:Stop()
gui:Destroy()
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)
---cleave
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local divergentFistANIM = game:GetService("ReplicatedStorage").Animations.Itadori.Cleave
local NewAnim = game:GetService("ReplicatedStorage").Animations.Heian.Cleave

local SWAP_DELAY = 0.1
local PLAY_DURATION = 0.2

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		animTracks.anim1.TimePosition = 0		
animTracks.anim1:Play()
animTracks.anim1.TimePosition = 0.6
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local camera = game.Workspace.CurrentCamera


local slowZoomSpeed = 0.2
local fastZoomSpeed = 0.7
local zoomInFOV = 100
local defaultFOV = 70


local function zoomIn()
    local zoomInTween = TweenService:Create(
        camera,
        TweenInfo.new(slowZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.In),
        {FieldOfView = zoomInFOV}
    )
    zoomInTween:Play()
end


local function zoomOut()
    local zoomOutTween = TweenService:Create(
        camera,
        TweenInfo.new(fastZoomSpeed, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
        {FieldOfView = defaultFOV}
    )
    zoomOutTween:Play()
end
task.wait(0)
        zoomIn()
		sounds.S1:Play()
               animTracks.anim1:AdjustSpeed(1.2)
local Sound = 97960256382964
local Volume = 5
local Speed = 1

s= Instance.new("Sound", game.Players.LocalPlayer.Character.HumanoidRootPart)
s.SoundId = "rbxassetid://" .. Sound
s:Play()
s.Volume = Volume
s.PlaybackSpeed = Speed
task.wait(0)
local Ken = game.ReplicatedStorage.Utils.Itadori.EyeTrails.Eye1

local ken = Ken:Clone()
ken.Parent = game.Players.LocalPlayer.Character:WaitForChild("Head")
ken.CFrame = ken.CFrame * CFrame.new(0, 0, 0)
for _, child in ipairs(ken:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(0)
local Sigma = game.ReplicatedStorage.Utils.Itadori.EyeTrails.Eye1

local sigma = Sigma:Clone()
sigma.Parent = game.Players.LocalPlayer.Character:WaitForChild("Head")
sigma.CFrame = sigma.CFrame * CFrame.new(0, 0, 0)
for _, child in ipairs(sigma:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Enabled = true
        child:Emit(5) 
    end
end
task.wait(0)
local Test = game.ReplicatedStorage.Utils.Preload.CursedInstinctsFX.r_VFX.Middle

local test = Test:Clone()
test.Parent = game.Players.LocalPlayer.Character:WaitForChild("Right Arm")

for _, child in ipairs(test:GetChildren()) do
    if child:IsA("ParticleEmitter") then
child.Color = ColorSequence.new(Color3.fromRGB(139, 0, 0)) 
child.Enabled = true
        child:Emit(5) 
    end
end
		task.wait(0.1)
	local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local camera = workspace.CurrentCamera  -- Reference to the camera


local dashSpeed = 75
local dashDuration = 1
local isDashing = false


local dashAnim = Instance.new("Animation")
dashAnim.AnimationId = "rbxassetid://0"
local animTrack = humanoid:LoadAnimation(dashAnim)
 

local function moveForward()
    if isDashing then return end
    isDashing = true

    wait(0)

    local bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.MaxForce = Vector3.new(100000, 100000, 100000)  -- Max force to apply the movement
    bodyVelocity.Parent = humanoidRootPart

    local dashEndTime = tick() + dashDuration  -- Time when dash should end

    while tick() < dashEndTime do
       
        local cameraDirection = camera.CFrame.LookVector
        local dashDirection = Vector3.new(cameraDirection.X, 0, cameraDirection.Z).unit

        bodyVelocity.Velocity = dashDirection * dashSpeed

        wait(0.05)  
    end

 
    animTrack:Stop()

  
    bodyVelocity:Destroy()
    isDashing = false
end


moveForward()  
	task.wait(0.7)
	      animTracks.anim1:Stop()
test:Destroy()
ken:Destroy()
sigma:Destroy()
zoomOut()
		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == divergentFistANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

humanoid.AnimationPlayed:Connect(onAnimationPlayed)
--ult m1
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local crushingBlowANIM = game:GetService("ReplicatedStorage").Animations.Itadori.MeleeUlt.Melee1
local NewAnim = game:GetService("ReplicatedStorage").Animations.Haruta.Ambush

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
		sounds.S1:Play()
               animTracks.anim1:AdjustSpeed(1.1)
		task.wait(1)
		animTracks.anim1:Stop()
		task.wait(0)

		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)
--m2 ult
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local crushingBlowANIM = game:GetService("ReplicatedStorage").Animations.Itadori.MeleeUlt.Melee2
local NewAnim = game:GetService("ReplicatedStorage").Animations.Mechamaru.Melee.Melee3

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
		sounds.S1:Play()
		task.wait(1)
		animTracks.anim1:Stop()
		task.wait(0)

		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

--m3 ult
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local crushingBlowANIM = game:GetService("ReplicatedStorage").Animations.Itadori.MeleeUlt.Melee3
local NewAnim = game:GetService("ReplicatedStorage").Animations.Nanami.Melee.Melee2

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
		sounds.S1:Play()
		task.wait(1)
		animTracks.anim1:Stop()
		task.wait(0)

		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

--m4 ult
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local ChatService = game:GetService("Chat")

local crushingBlowANIM = game:GetService("ReplicatedStorage").Animations.Itadori.MeleeUlt.Melee4
local NewAnim = game:GetService("ReplicatedStorage").Animations.Nanami.CleavingWhirlwind

local SWAP_DELAY = 0
local PLAY_DURATION = 0

local isAnimationSequenceActive = false


local function playAnimationSequence(player)
	if isAnimationSequenceActive then return end
	isAnimationSequenceActive = true

	local character = player.Character
	if not character then return end

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end

	
	local animations = {
		anim1 = Instance.new("Animation")
	}
	local sounds = {
		S1 = Instance.new("Sound", character),
		S2 = Instance.new("Sound", character)
	}
	
	sounds.S1.SoundId = ""
	sounds.S2.SoundId = ""
	
	animations.anim1.AnimationId = NewAnim.AnimationId

	
	local animTracks = {
		anim1 = humanoid:LoadAnimation(animations.anim1)
	}

	
	for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
		track:Stop()
	end

	task.spawn(function()
		
		
		--MOVESET START
		
		
		animTracks.anim1:Play()
animTracks.anim1.TimePosition = 0.6
		sounds.S1:Play()
animTracks.anim1:AdjustSpeed(1)
		task.wait(1)
		animTracks.anim1:Stop()
		task.wait(0)

		--MOVESET END
		
		
		task.wait(PLAY_DURATION)
		isAnimationSequenceActive = false

		for _, track in pairs(animTracks) do
			track:Stop()
		end
	end)
end

local function onAnimationPlayed(animationTrack)
	if animationTrack.Animation.AnimationId == crushingBlowANIM.AnimationId then
		playAnimationSequence(Players.LocalPlayer)
	end
end

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
humanoid.AnimationPlayed:Connect(onAnimationPlayed)
local localPlayer = game:GetService("Players").LocalPlayer

local char = game.Players.LocalPlayer.Character
char.Humanoid.AnimationPlayed:Connect(function(a)
if a.Animation.AnimationId == "rbxassetid://120133391090244" then
local Anim = Instance.new("Animation")
Anim.AnimationId = "rbxassetid://100298754986980"
local Idle = char.Humanoid:LoadAnimation(Anim)
Idle:Play(0.3)
Idle.TimePosition = 1
Idle.Priority = Enum.AnimationPriority.Idle
Idle:AdjustSpeed(0.0001)
a.Stopped:Wait()
Idle:Stop(0.3) 
end
end)
local localCharacter = localPlayer.Character or localPlayer.CharacterAdded:Wait()
